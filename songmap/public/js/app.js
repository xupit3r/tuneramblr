// general application stuff
var APP = {};

APP.onload = function () {
	// anything that needs to be
	// handled onload of DOM goes
	// in here
	
	// setup the map
	SONGMAP.initMap("map",40.00,79.00,6);
}