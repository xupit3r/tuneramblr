var HANDLERS = {};

// handlers for each section
// of the site
HANDLERS.functions = {};
HANDLERS.metaside = {};
HANDLERS.tag_cloud = {};

HANDLERS.functions.setup = function() {
	$("#fn_login").click(function () {
		// present login form
	});
	
	$("#fn_settings").click(function () {
		// bring up settings page
	});

};

HANDLERS.functions.reload = function() {

};

HANDLERS.playlists.setup = function() {
	// the elements that make up this 
	// section are going to dictate
	// what goes in here...
	// not sure yet

};

HANDLERS.playlists.reload = function() {

};

HANDLERS.songs.setup = function() {
	// the elements that make up this 
	// section are going to dictate
	// what goes in here...
	// not sure yet
	
	// this seems likely to be links
	// only, but not sure what I am going 
	// to do with them

};

HANDLERS.songs.reload = function() {

};